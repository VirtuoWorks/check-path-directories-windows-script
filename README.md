# Check Path Directories for Windows CMD

This small windows cmd script checks if each PATH registered in the Windows %PATH% variable exists.

Usage :

* Open Windows CMD;
* Execute : `CheckPathDirectories.bat`